def jarName = "socks_netty_v1.0"

task makeJar(type: Copy) {
    delete 'build/libs/' + jarName + ".jar"
    from("build/intermediates/aar_main_jar/release")
    into('build/libs/')
    include('classes.jar')
    rename('classes.jar', jarName + ".jar")
}
makeJar.dependsOn(build)
